import { ReducelengthPipe } from './reducelength.pipe';

describe('ReducelengthPipe', () => {
  it('create an instance', () => {
    const pipe = new ReducelengthPipe();
    expect(pipe).toBeTruthy();
  });
});
