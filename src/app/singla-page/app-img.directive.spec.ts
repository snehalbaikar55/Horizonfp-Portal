import { AppImgDirective } from './app-img.directive';

describe('AppImgDirective', () => {
  it('should create an instance', () => {
    const directive = new AppImgDirective();
    expect(directive).toBeTruthy();
  });
});
