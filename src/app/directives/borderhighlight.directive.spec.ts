import { BorderhighlightDirective } from './borderhighlight.directive';

describe('BorderhighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderhighlightDirective();
    expect(directive).toBeTruthy();
  });
});
